name: conduit_coding_test
description: "Conduit Coding Test"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # decimal: 정밀한 십진수 계산을 위한 패키지 (v3.1.0)
  decimal: ^3.1.0

  # Mock library
  mocktail: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true